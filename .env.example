# No required environment variables for a normal install.
# Configure API key and runtime behavior in the app Settings UI.

# Optional first-run defaults (settings can still be changed later in-app):
# AGENT_CONTEXT_SCREENSHOT_TTL_DAYS=3
# AGENT_CONTEXT_AUDIO_TTL_DAYS=3
