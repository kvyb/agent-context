import Foundation

final class MicActivityMonitor: @unchecked Sendable {
    // vNext intentionally uses explicit transcript controls only.
    // No automatic microphone/call detection pipeline is active.
}
